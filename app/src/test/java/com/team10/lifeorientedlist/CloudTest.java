package com.team10.lifeorientedlist;

/*import com.google.firebase.firestore.FirebaseFirestore;

import org.junit.BeforeClass;
import org.junit.Test;

import static com.google.firebase.iid.FirebaseInstanceId.getInstance;
import static junit.framework.TestCase.assertEquals;

public class CloudTest {
 public static final String FIREBASE_URL = "https://lifeorientedlist.firebaseio.com/";
//I'm here: https://cloud.google.com/solutions/mobile/mobile-firebase-app-engine-flexible#adding_a_service_account_to_the_firebase_project
    private static final String NAME = "Test NAME";
    private static final int    POINTS = 100;
    private static final String CREATION_TIME = "Now";
    private static final String DUE_TIME = "Later";
    private static final boolean DONE = true;

    private static Task loadTask;
    private static Task saveTask;
    FirebaseFirestore db = FirebaseFirestore.getInstance();




    public CloudTest() {
        saveTask = new Task();

        saveTask.setName(NAME);
        saveTask.setPoints(POINTS);
        saveTask.setTimeCreated(CREATION_TIME);
        saveTask.setTimeDue(DUE_TIME);
        saveTask.setDone(DONE);

    }
    @Test
   public void addDatabase() {assertEquals(NAME, saveTask.getName());}
    @BeforeClass
    public static void createTaskForTesting() {

    }


}*/
